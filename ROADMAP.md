# NekoMusicPlayer Roadmap

> 最近更新：2026-08-13  
> 维护方式：每个发布周期评审一次；只有已关联 Issue、负责人、版本和验收标准的事项才视为承诺。

## 规划原则

- `Now` 仅包含已经具备明确交付条件的工作。
- `Next` 是下一周期优先评审池，不代表已经承诺版本或日期。
- `Later` 保存有价值但尚未排期的方向。
- `Not planned` 明确当前不接受的范围，需求变化时可重新评审。
- 每个版本预留约 20% 容量用于缺陷、依赖升级和外部接口变化。
- 新的数据源、远程同步和诊断能力必须先评估版权、隐私、安全及服务条款风险。

## Now

### 0.3.0 发布候选验证

状态：本地完成，等待远端激活。

- 推送当前阶段提交并观察首次 GitHub Actions。
- 配置 `SSH_KNOWN_HOSTS`、`production` Environment 和分支保护。
- 完成生产部署、烟雾测试与回滚确认。
- 在已验证提交上创建 `v0.3.0` 标签和 GitHub Release。

完成条件以 [0.3.0 发布检查记录](./docs/RELEASE_CHECKLIST_0.3.0.md)为准。

## Next

以下为下一周期的优先评审主题，尚未形成版本承诺：

1. **0.3.x 稳定性维护**
   - 收集真实环境中的播放、PWA 更新、缓存和外部适配器缺陷。
   - 评估依赖安全更新与浏览器兼容性。
   - 为生产部署建立可重复的烟雾检查记录。
2. **可访问性专项**
   - 盘点键盘导航、焦点顺序、屏幕阅读器标签和颜色对比度。
   - 形成可测量的验收清单后再决定版本范围。
3. **缓存治理**
   - 评估缓存上限、淘汰策略、按来源清理和空间不足提示。
   - 明确旧数据兼容和迁移方案。
4. **适配器可维护性**
   - 定义统一的健康状态、错误分类和契约 Fixture。
   - 外部服务真实烟雾检查与提交必过测试保持隔离。

## Later

以下方向需要用户价值、维护成本和风险评审后才能进入 `Next`：

- 移动端和小屏响应式体验专项。
- Media Session API、系统媒体键和锁屏控制。
- 歌词获取、解析、同步及离线缓存。
- 播放历史、收藏、最近播放和智能列表。
- 可选的跨设备同步或云端备份。
- 更多来源适配器。
- 用户可控的诊断导出和匿名性能指标。
- 更严格的 Content Security Policy 与代理目标白名单。

## Not planned

当前明确不纳入计划：

- 未经授权下载、破解 DRM 或绕过平台访问控制。
- 在源码、日志、测试附件或客户端分发包中内置账号凭据与 Cookie。
- 未提供数据导出、删除和隐私说明的强制云端同步。
- 在没有稳定接口、使用授权和维护责任人的情况下承诺新数据源。
- 为追求通过率而关闭类型检查、批量跳过测试或取消 SSH 主机校验。

## 需求进入计划的条件

需求由对应 Issue 模板提交，评审时至少确认：

- 用户问题、使用场景和预期价值。
- 范围、非目标与交互说明。
- 数据迁移、兼容性和外部依赖影响。
- 安全、版权、隐私与服务条款风险。
- 可验证的验收标准和测试方案。
- 唯一负责人、目标 Milestone 和优先级。

排序参考用户价值、覆盖人数、实现成本、维护风险和外部依赖风险。未满足以上条件的事项保留为候选，不写入已承诺版本。

## 建议远端配置

仓库维护者在推送后建立：

- Milestone：`0.3.x 稳定性维护`、`0.4.0 下一功能版本`、`Backlog`。
- 标签：`type:bug`、`type:feature`、`type:adapter`、`type:debt`、`priority:p0`～`priority:p3`、`status:needs-triage`、`risk:external-api`。
- 每个 `Now` 项必须关联一个 Issue；没有负责人和验收标准的 Issue 不进入 Milestone。

